type FooterUrl = {
  text: string;
  href: string;
};

export type FooterLinkSection = {
  heading: string;
  urls: FooterUrl[];
};

export type serviceDataType = {
    image:string;
    icon:string;
    title:string;
    text:string;
}